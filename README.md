# tree-store
Install deps using [`npm`](https://www.npmjs.com/package/jest):

```bash
npm install
```

Run tests:

```bash
npm run test
```

Also you can compile TypeScript to JS: 

```bash
npm run build
```
